"""BestBuy connector (stub).

Fill this in when you have access to an API or HTML parsing logic.
Keep networking + parsing here; keep ranking/business logic in services/.
"""

from __future__ import annotations

from typing import Any


def search_bestbuy(query: str, api_key: str | None, max_results: int = 10) -> list[dict[str, Any]]:
    # TODO: implement API call (and add tests + fixtures)
    return []
